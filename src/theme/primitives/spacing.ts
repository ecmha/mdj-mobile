export const p = (padding: number | undefined) => ({
  padding,
});

export const pr = (padding: number | undefined) => ({
  paddingRight: padding,
});

export const pl = (padding: number | undefined) => ({
  paddingLeft: padding,
});

export const px = (padding: number | undefined) => ({
  paddingHorizontal: padding,
});

export const pt = (padding: number | undefined) => ({
  paddingTop: padding,
});

export const pb = (padding: number | undefined) => ({
  paddingBottom: padding,
});

export const py = (padding: number | undefined) => ({
  paddingVertical: padding,
});

export const m = (margin: number | undefined) => ({
  margin,
});

export const mr = (margin: number | undefined) => ({
  marginRight: margin,
});

export const ml = (margin: number | undefined) => ({
  marginLeft: margin,
});

export const mx = (margin: number | undefined) => ({
  marginLeft: margin,
  marginRight: margin,
});

export const mt = (margin: number | undefined) => ({
  marginTop: margin,
});

export const mb = (margin: number | undefined) => ({
  marginBottom: margin,
});

export const my = (margin: number | undefined) => ({
  marginTop: margin,
  marginBottom: margin,
});
