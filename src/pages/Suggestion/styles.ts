import { StyleSheet } from "react-native";

export default StyleSheet.create({
  input: {
    height: 45,
  },
  textArea: {
    height: 180,
  },
  button: {
    height: 50,
    justifyContent: "center",
    alignItems: "center",
  },
});