import SettingsLayout from '@/layouts/SettingsLayout';
import { Text } from 'react-native';

export default function Settings() {
  return (
    <SettingsLayout pageTitle="Paramètres">
      <Text>Paramètres</Text>
    </SettingsLayout>
  );
}
