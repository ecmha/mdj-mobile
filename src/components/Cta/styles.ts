import { StyleSheet } from "react-native";

export default StyleSheet.create({
    cta: {
       height: 50,
       width: 50,
       borderRadius: 25,
       justifyContent: 'center',
       alignItems: 'center',       
    }
});